import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const EditTodoScreen = ({navigation}) => {
  return <Text>Edit Todo Screen</Text>;
};

const styles = StyleSheet.create({
  container: {},
});

export default EditTodoScreen;
