import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const AddTodoScreen = ({navigation}) => {
  return <Text>Add Todo Screen</Text>;
};

const styles = StyleSheet.create({
  container: {},
});

export default AddTodoScreen;
